import './globals.css';
export const metadata = { title: 'Ogundipe Samson — Journalist / Scriptwriter' };
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
